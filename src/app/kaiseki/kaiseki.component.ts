import { Component } from '@angular/core';

@Component({
  selector: 'app-kaiseki',
  standalone: true,
  templateUrl: './kaiseki.component.html',
  styleUrls: ['./kaiseki.component.scss']  // Let op: dit moet 'styleUrls' zijn, niet 'styleUrl'
})
export class KaisekiComponent {
  // Voeg hier je logica toe
}
